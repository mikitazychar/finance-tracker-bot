# Finance Tracker Bot 💰📉

Telegram-бот для управления личными финансами. Проект реализован с использованием архитектурных паттернов, разделением ответственности и хранением данных в SQLite.

## ! ВАЖНО:
    Бот находится в разработке, часть структуры и функционала не реализованы или не ипользуются.


## 🚀 Функционал
- **Учет доходов:** Установка общего бюджета пользователя.
- **Логирование расходов:** Запись трат по категориям через пошаговый диалог.
- **Машина состояний (FSM):** Интуитивно понятный интерфейс с защитой от некорректного ввода.
- **Статистика:** Подробный отчет о балансе, проценте потраченных средств и истории последних операций.
- **Безопасность:** Использование окружения `.env` для защиты API-токенов.

## 🛠 Технологии
- **Python 3.10+**
- **Aiogram 3.x** (Асинхронная библиотека для Telegram API)
- **SQLite3** (Встроенная реляционная база данных)
- **Environs** (Управление конфигурацией)
- **Logging** (Отслеживание состояния системы)

## 📁 Структура проекта
Проект организован по модульному принципу для упрощения масштабирования:
- `config_data/` — конфигурация бота и загрузка переменных окружения.
- `database/` — уровень доступа к данным (DAO) и работа с SQLite.
- `handlers/` — логика обработки сообщений (команды, FSM, статистика).
- `keyboards/` — сборка интерактивных меню.
- `states/` — описание состояний пользователя (FSM).
- `loader.py` — инициализация основных объектов (Bot, Dispatcher).
- `main.py` — точка входа в приложение.

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/ваш-логин/finance-tracker-bot.git](https://github.com/mikitazychar/finance-tracker-bot.git)
   cd finance-tracker-bot

2. **Создайте и активируйте виртуальное окружение:**
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # macOS/Linux:
    source venv/bin/activate

3. **Установите зависимости:**
    pip install -r requirements.txt

4. **Настройте переменные окружения:**
    Создайте файл .env в корневой папке.
    Используйте .env.template как пример.

5. **Запустите бота:**
    python main.py
